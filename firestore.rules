rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Bloquea todo el tráfico público. Tu servidor backend seguirá teniendo acceso total.
      allow read, write: if false; 
    }
  }
}